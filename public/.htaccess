RewriteEngine On
Options -MultiViews

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^ https://lokal-userpanel.mpeoplesnet.com%{REQUEST_URI} [R=301,L]

# React Routing Fix
RewriteBase /

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]